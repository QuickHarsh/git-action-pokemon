name: Node Setup
on:
    push:
        branches: [main]
jobs:
    setup-node:
      runs-on: ubuntu-latest
      
      defaults:
          run:
              working-directory: backend

      steps:
          - name: Checkout Repo
            uses: actions/checkout@v4

          - name: Setup Node
            uses: actions/setup-node@v4
          
      
